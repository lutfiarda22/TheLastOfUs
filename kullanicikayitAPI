<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kayıt Ol</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #0f0f0f;
            background-image: radial-gradient(circle at center, #2a2a2a 0%, #000000 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        .container {
            background-color: rgba(20, 20, 20, 0.9);
            padding: 40px;
            border-radius: 15px;
            width: 380px;
            border: 2px solid #4CAF50;
            box-shadow: 0 0 25px rgba(76, 175, 80, 0.4);
            text-align: center;
        }

        h2 {
            margin-bottom: 30px;
            font-size: 28px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            font-size: 14px;
            color: #b0b0b0;
        }

        input {
            width: 100%;
            padding: 12px;
            background-color: #1a1a1a;
            border: 1px solid #333;
            border-radius: 6px;
            color: white;
            margin-top: 6px;
        }

        input:focus {
            outline: none;
            border-color: #4CAF50;
            box-shadow: 0 0 8px rgba(76, 175, 80, 0.3);
        }

        .btn-submit {
            width: 100%;
            padding: 14px;
            background-color: #4CAF50;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            color: white;
            cursor: pointer;
            font-weight: bold;
        }

        .btn-submit:hover {
            background-color: #45a049;
        }

        .msg {
            display: none;
            margin-top: 12px;
            font-size: 14px;
        }

        .error {
            color: #ff4444;
        }

        .success {
            color: #4CAF50;
        }

        .footer-link {
            margin-top: 20px;
            font-size: 14px;
        }

        .footer-link a {
            color: #4CAF50;
            text-decoration: none;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Kayıt Ol</h2>

        <form id="registerForm">
            <div class="form-group">
                <label for="username">Kullanıcı Adı</label>
                <input type="text" id="username" placeholder="örn. Joel_Miller" required>
            </div>

            <div class="form-group">
                <label for="email">E-posta</label>
                <input type="email" id="email" placeholder="E-posta adresin" required>
            </div>

            <div class="form-group">
                <label for="password">Şifre</label>
                <input type="password" id="password" placeholder="Şifre" required>
            </div>

            <div class="form-group">
                <label for="confirm">Şifre (Tekrar)</label>
                <input type="password" id="confirm" placeholder="Şifre tekrar" required>
            </div>

            <button type="submit" class="btn-submit">Kayıt Ol</button>

            <div id="error" class="msg error"></div>
            <div id="success" class="msg success">Kayıt başarılı! 🎉</div>
        </form>

        <div class="footer-link">
            Hesabın var mı? <a href="giris.html">Giriş Yap</a>
        </div>
    </div>

    <script>
        const form = document.getElementById("registerForm");
        const errorMsg = document.getElementById("error");
        const successMsg = document.getElementById("success");

        form.addEventListener("submit", async function (e) {
            e.preventDefault();

            errorMsg.style.display = "none";
            successMsg.style.display = "none";

            const username = document.getElementById("username").value.trim();
            const email = document.getElementById("email").value.trim();
            const password = document.getElementById("password").value.trim();
            const confirm = document.getElementById("confirm").value.trim();

            // Basit doğrulamalar
            if (password.length < 8) {
                showError("Şifre en az 8 karakter olmalıdır.");
                return;
            }

            if (password !== confirm) {
                showError("Şifreler eşleşmiyor.");
                return;
            }

            // API isteği
            try {
                const response = await fetch("http://127.0.0.1:8000/api/register/", {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json",
                    },
                    body: JSON.stringify({
                        username: username,
                        email: email,
                        password: password
                    })
                });

                const data = await response.json();

                if (!response.ok) {
                    showError(data.error || "Kayıt sırasında bir hata oluştu.");
                } else {
                    successMsg.style.display = "block";
                    form.reset();
                }

            } catch (err) {
                showError("Sunucuya bağlanılamadı.");
            }
        });

        function showError(msg) {
            errorMsg.innerText = msg;
            errorMsg.style.display = "block";
        }
    </script>

</body>
</html>
